apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: percona-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # Установите подходящий размер хранилища
  storageClassName: standard  # Укажите свой StorageClass, если необходимо
